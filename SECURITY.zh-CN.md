# 安全说明

> Aegis SSH 将 SSH 凭据与常规 AI Agent 接口隔离，但不会把任意远程 Shell 访问变成沙箱。

[项目首页](README.zh-CN.md) · [服务器配置](docs/server-setup.zh-CN.md) · [Agent 使用](docs/agent-usage.zh-CN.md) · [运维](docs/operations.zh-CN.md) · **安全** · [English](SECURITY.md)

## 安全边界

| Aegis SSH 保护的内容 | 不在保证范围内 |
| --- | --- |
| 凭据在本地加密存储 | 获准执行任意 Shell 的对抗性 Agent |
| 机密信息通过 `/dev/tty` 输入 | 与 broker 同 OS 用户的恶意代码 |
| MCP schema 和 CLI 参数不包含连接凭据 | 已被入侵的本机或 SSH 服务器 |
| 每次连接都校验已固定的主机密钥 | 编码后绕过静态分析或脱敏的数据 |
| 本机审批、输出过滤和审计记录 | 未提前启用恢复时找回已丢失的主密码 |

导入的私钥内容和口令保存在加密 vault 中，程序不保存私钥源路径。daemon 持有解密后的凭据并完成 SSH 认证；Agent 只能获得公开别名和过滤后结果。

> [!IMPORTANT]
> 本项目提供凭据隔离和最佳努力的数据披露控制，不是远程 Shell 沙箱。

## 已接受的限制

获准执行任意远程 Shell 的 Agent 可能构造绕过静态分析的命令，或将敏感数据编码为输出过滤器无法识别的形式。本机审批和脱敏能降低意外泄露，但无法保证阻止有意规避策略的命令作者。

与 daemon 运行在同一本地 OS 用户下的恶意进程，可能读取进程内存、访问 Unix socket、追踪 daemon 或修改用户文件。如果需要防御同用户本地代码，应使用独立 OS 账户或更强的主机隔离。

本机审批可阻止 Agent 通过 MCP 自行批准，但无法证明是谁在操作本地终端。必须保护本地账户，并在批准前核对别名、风险类别和完整命令。

## 安全操作规则

- 保护 `~/.aegis-ssh`、带时间戳的恢复归档、备份、恢复码和本地用户账户。
- 使用 `aegis-ssh lock` 清除内存凭据；不再需要 broker 时使用 `aegis-ssh stop`。
- 添加服务器前，通过独立可信渠道校验主机密钥指纹。
- 不得通过参数、环境变量、Agent 提示词、MCP 配置、工单或对话传递凭据和恢复材料。
- 将所有 `[REDACTED:...]` 标记和截断警告视为最终结果。
- 只能在 broker 停止时，将 `config.yaml`、`vault.enc` 和 `recovery.enc` 作为同一组备份。
- 只在私密终端使用 `server password <别名>`；终端录屏和剪贴板工具可能捕获显示的密码。

## 安全诊断材料

| 人工复核后通常可分享 | 严禁分享 |
| --- | --- |
| `aegis-ssh status` | `vault.enc`、`recovery.enc` 或归档 vault |
| `server list` 中的公开别名 | 主密码、恢复码、SSH 密码或私钥口令 |
| 稳定错误码 | 私钥、口令或私钥源路径 |
| 已复核的运维日志 | 进程内存、core dump、调试器输出或 socket 流量 |
| 已脱敏的审计元数据 | 添加服务器或查看密码时的终端录屏 |
| 原样保留的脱敏标记 | 触发敏感审批的未脱敏远程输出 |

私有别名和命令预览仍可能识别基础设施或业务。公开报告时必须按需删除。

## 漏洞报告

不得在 issue 中公开真实凭据、主机、地址、审计记录、恢复码或 vault 文件。只创建包含最小化、已脱敏复现步骤的公开 issue，并请求维护者提供私密报告渠道。

报告应包含：

- 受影响的版本和操作系统；
- 已脱敏的复现步骤；
- 预期与实际安全行为；
- 是否可以在不使用真实基础设施的情况下复现。

---

[返回项目首页](README.zh-CN.md) · [运维参考](docs/operations.zh-CN.md)
