# 安全说明

[简体中文] | [English](SECURITY.md)

## 安全边界

Aegis SSH 用于防止密码和私钥 SSH 连接信息通过常规 AI Agent 接口泄露。凭据在本地加密存储，通过 `/dev/tty` 交互式输入或导入，解密后由 daemon 持有，不会进入 MCP schema、CLI 参数、日志或公开结果类型。导入的私钥内容和私钥口令保存在加密 vault 中，程序不会保存私钥源路径。SSH 主机密钥会在添加服务器时固定，每次连接都会校验。

本项目提供的是凭据隔离和最佳努力的数据披露控制，不是远程 Shell 沙箱。

## 已接受的限制

获准执行任意远程 Shell 的 Agent，可能构造绕过静态分析的命令，或将敏感数据编码为输出过滤器无法识别的形式。批准和脱敏可以降低意外泄露的风险，但无法保证能够阻止有意规避策略的命令作者。

与 daemon 运行在同一本地 OS 用户下的恶意进程，可能读取进程内存、访问用户的 Unix socket、追踪 daemon 或修改用户文件。如果需要防御同用户本地代码，应将 broker 运行在独立 OS 账户下，或使用更强的主机隔离机制。

broker 无法在密码学上证明对话中的批准回复一定来自人类。配套 Skill 会要求 Agent 等待用户输入精确回复，审计日志会记录批准生命周期。

## 安全操作规则

- 妥善保护 `~/.aegis-ssh`、备份文件和本地用户账户。
- 不需要 broker 时，执行 `aegis-ssh lock` 停止 daemon 并清除内存凭据。
- 添加服务器时，必须通过可信渠道核对主机密钥指纹。
- 不得通过命令行参数、环境变量、Agent 提示词或 MCP 配置传递密码、私钥、私钥路径或私钥口令。
- 将脱敏标记视为最终结果，不得要求 Agent 绕过脱敏。
- 只能在 daemon 停止时，将 `config.yaml` 和 `vault.enc` 作为同一组备份。

## 安全诊断材料

可以分享的内容仅限于：

- `aegis-ssh status` 的输出
- `aegis-ssh server list` 中经过复核的公开别名
- 已脱敏的错误代码
- 经人工复核、仅包含别名、命令哈希或有界预览、策略决策和脱敏计数的审计行

所有 `[REDACTED:...]` 标记都必须原样保留。

严禁分享：

- `vault.enc`
- 主密码或 SSH 密码
- 私钥、私钥口令或私钥源路径
- 进程内存、core dump 或调试器输出
- Unix socket 流量
- 添加服务器时的终端录屏
- 触发敏感审批的未脱敏远程输出

如果私有别名或命令预览能够暴露基础设施或业务信息，在公开报告中也必须删除。

## 漏洞报告

不要在公开漏洞报告中提交真实凭据、主机名、IP 地址、审计记录或 vault 文件。

如果项目已提供维护者的私密联系方式，应首先通过私密渠道报告。如果没有私密联系方式，只创建包含最小化、已脱敏复现步骤的公开 issue，并请求维护者提供私密沟通渠道。
